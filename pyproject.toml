[project]
name = "course-work-object-oriented-programming"
version = "0.1.0"
description = "course_work"
authors = [
    {name = "Василий",email = "kulikov821999@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest (>=9.0.2,<10.0.0)",
    "coverage (>=7.12.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.lint.dependencies]
flake8 = "^7.3.0"
mypy = "^1.19.0"
isort = "^7.0.0"


[tool.poetry.group.devtools.dependencies]
black = "^25.11.0"
pytest = "^9.0.2"
coverage = "^7.12.0"

